// Matches a standalone country code like +91.
export const COUNTRY_CODE_PATTERN = /^\+\d{1,4}$/

// Matches complete phone input persisted by form normalizers.
export const COUNTRY_CODE_PHONE_PATTERN = /^\+\d{1,13}$/
